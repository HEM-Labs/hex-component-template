# Third-Party Notices

This file is an example. Before releasing a component, replace these examples with the actual third-party software that the image bundles, redistributes, or materially depends on at runtime. Delete sections that do not apply, and add one section per upstream project, base image, or runtime dependency.

## Example: Bundled Upstream Application

Use this section when the component image bundles a complete upstream application.

- Name:
- Website:
- Repository:
- Upstream author/maintainer:
- Reported upstream license:
- Default bundled version, branch, tag, or commit:

This container image bundles the upstream application listed above. The Hex packaging in this repository does not remove or replace the upstream license obligations for the bundled application.

## Example: Distribution or Runtime Base Image

Use this section when the component is built from a third-party distribution image or runtime image.

- Name:
- Documentation:
- Repository:
- Image:

This repository builds on the base image listed above and adds Hex-specific packaging, configuration, or runtime integration.

## Example: Installed Runtime Dependency

Use this section when the image installs a third-party runtime dependency in addition to the packaged component.

- Name:
- Website:
- Repository:
- Reported upstream license:
- Installed by:

This image installs the dependency listed above as a runtime dependency for the packaged component.
